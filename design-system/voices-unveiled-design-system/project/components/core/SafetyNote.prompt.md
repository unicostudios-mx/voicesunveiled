One-line: The required care/consent notice shown wherever student stories or images appear — defaults to the canonical brand wording.

```jsx
<SafetyNote />
```

Sage-tinted note with a shield glyph. Pass `children` to override copy, but the
default ("Some names, images, or identifying details may be changed to protect
student safety.") is the approved language. Always include this on story and
testimonial pages.
