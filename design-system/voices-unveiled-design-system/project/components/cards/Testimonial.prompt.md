One-line: A hero testimonial block — large serif quote with a terracotta quote mark and minimal attribution, optionally followed by a CTA.

```jsx
<Testimonial
  quote="I was not confident enough to speak. Now I believe I am valuable, precious, and need to be heard."
  name="Ehsaneh, 28"
  cta={<Button variant="primary">Support more students like Ehsaneh</Button>}
/>
```

Grounds: `ivory` (default) · `sage` (care contexts) · `dark` (midnight,
institutional). Treat testimonials as a primary emotional asset; one per section.
